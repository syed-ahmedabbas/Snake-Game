# 🐍 EOD Report — Snake Game | April 20, 2026

**Repo:** `syed-ahmedabbas/Snake-Game`
**Language:** C++ | **Framework:** Raylib
**File Modified:** `src/main.cpp`

---

## ✅ What Was Done Today

### 1. Core Game Architecture Established
Designed and implemented a clean, class-based architecture for the Snake Game using Raylib. The codebase is organized around three main classes with clear separation of concerns:

| Class | Responsibility |
|-------|----------------|
| `Snake` | Body state, movement direction, segment growth, rendering |
| `Food` | Texture loading, random position generation, rendering |
| `Game` | Orchestrates snake + food, collision detection |

---

### 2. `Snake` Class — Implemented
- Used `std::deque<Vector2>` for the snake body — allows efficient `push_front` (add head) and `pop_back` (remove tail) for movement.
- Initial body spawned at grid positions `{6,9}`, `{5,9}`, `{4,9}` (3 segments, facing right).
- **Growth mechanic:** `addSegment` flag — when `true`, the tail is NOT popped on update, effectively growing the snake by 1.
- **Rendering:** Each segment drawn as a rounded rectangle (`DrawRectangleRounded`) in bright neon green `{57, 255, 20, 255}`.

```cpp
void Update(){
    body.push_front(Vector2Add(body[0], direction)); // move head
    if(addSegment == true){ addSegment = false; }    // grow
    else { body.pop_back(); }                        // normal move
}
```

---

### 3. `Food` Class — Implemented
- Loads a custom food texture from `graphics/food.png` (resized to 20×20px).
- **Collision-aware spawning:** `GenerateRandomPos()` loops until a cell not occupied by the snake body is found — uses `ElementInDeque()` helper.
- Texture is properly unloaded in the destructor (`~Food()`) to prevent memory leaks.

---

### 4. `Game` Class — Implemented
- Owns both `Snake` and `Food` instances.
- `CheckCollisionWithFood()`: detects when the snake head (`body[0]`) lands on the food position → regenerates food position + sets `snake.addSegment = true`.

---

### 5. Global Utilities — Implemented
- **`ElementInDeque()`** — checks if a `Vector2` exists in a deque; used for food spawn validation.
- **`eventTriggered(interval)`** — time-based tick controller using `GetTime()`; currently drives snake updates every **0.2 seconds**.

---

### 6. Main Game Loop — Implemented
- Window: **600×600px** (20 cells × 30px per cell).
- Target FPS: **60**.
- Arrow key input with **reverse-direction prevention** (can't move directly into yourself).
- Background: dark forest green `{20, 60, 20, 255}`.
- Draw order: Food → Snake (food renders underneath).

```
[Input] → [eventTriggered check] → [game.Update()] → [BeginDrawing] → [game.Draw()] → [EndDrawing]
```

---

## 🔧 Architecture Decisions

- **`deque` over `vector`** for snake body: O(1) push/pop at both ends vs O(n) for vector.
- **Time-based updates** decoupled from FPS — smooth and frame-rate independent movement.
- **Texture-based food** instead of plain shapes — adds visual polish.
- **`Vector2Equals`** from `raymath.h` used for precise float grid comparisons.

---

## ⏳ Pending / Next Steps

- [ ] **Wall collision detection** — game over when snake exits grid bounds
- [ ] **Self-collision detection** — game over when snake head hits its own body
- [ ] **Score system** — track and display score on screen
- [ ] **Game over / restart screen** — UI state management
- [ ] **Sound effects** — eat food, game over
- [ ] **Speed scaling** — increase snake speed as score grows
- [ ] **Pause functionality** — toggle with spacebar

---

## 📌 Commit Summary

```
feat: initialize snake game project with VS Code configuration and base implementation
```

---

*EOD generated on 2026-04-20 at 22:34 PKT*
